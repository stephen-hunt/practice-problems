#include <iostream>

#include "config.hpp"

int main () {
    std::cout << "Version " << SwapBits_VERSION_MAJOR << "." << SwapBits_VERSION_MINOR << std::endl;
    std::cout << "Hello World! Time to swap!";
    return 0;
}